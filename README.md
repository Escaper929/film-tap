# film-tap

碰一下贴在机身上的 NFC 挂件，就知道这台相机里装的什么胶卷、装了多久、还剩几张。

## 它解决什么

胶片一卷能拍很久，几台机身轮着用的时候，很容易忘记某台机器里装的是什么卷、
什么时候装的、还剩几张没拍完。

这个 App 把「机身 ↔ 当前胶卷」的状态记在手机上，靠一个挂在背带上的 NFC 挂件直达：

- 挂件里写一条 URL 记录：`https://<你的域名>/?c=<机身ID>`
- 手机碰一下 → 直接打开那台机身 → 看到当前卷、装了多少天、还剩几张
- 换卷只改 App 里的数据，**标签一辈子只写一次**

## 功能

- **机身管理** —— 名称、品牌、型号、画幅（135 / 120 / 110 / 大画幅）
- **机内胶卷** —— 装卷、张数、回卷自动归档
- **胶卷库存** —— 手上还有哪些卷、各几卷、什么时候到期；装卷时自动扣
- **推拉提示** —— 填了实拍 EI 自动算推拉档位，送冲扫时提醒你交代店家
- **装卷天数** —— 超过 180 天标「该冲了」，超过 365 天亮红
- **拍摄历史** —— 每台机身拍过的卷、各用了多少天
- **统计** —— 机身数、已用胶卷数、常用胶卷 Top 5
- **冲扫备注** —— 一键复制成能直接发给冲扫店的说明
- **备份** —— JSON 导出导入；可选同步到自己的 NAS（WebDAV）

> **这里不数快门。** 机身计数器上本来就能看到拍了多少张，
> 在手机里再记一遍只会多一个要维护、还大概率记错的地方。
> App 只记「这卷是什么、多少张、装了多少天」。每卷多少张仍然保留
> （`135 · 36 张`），因为那是冲扫和归档时要用的信息，和「已拍几张」是两回事。

## 数据与隐私

**所有数据默认只存在你这台设备的浏览器里**，没有服务器、没有账号。
存储键是 `filmtap.v1`。

> 项目原先叫 `film-nfc`，改名时存储键一并从 `filmnfc.v1` 换了过来。
> 代码里保留了**读取时自动接过来**的逻辑，所以改名前记的机身不会丢 ——
> 打开新版本时会自动把老键的数据搬到新键，之后一切以新键为准。

副本键是 `filmtap.v1.corrupt`，平时是空的，只在**主键读不出来**时才出现：

> 解析失败时如果直接当空地处理，界面上和「真的还没录过机身」长得一模一样，
> 用户接着随手记一台，就把坏掉的那份永久覆盖了。所以这时候会把原始文本原样留一份，
> 首页和设置页都会露一条横幅说清楚「没丢，可以导出来看看」，
> 并提供「导出原始数据」和「丢弃它」两个出口。留底只在第一次失败时写，
> 后面更残缺的那几次不会把最完整的那份盖掉。
>
> 另一头也收紧了：单台机身或单条历史记录坏掉时**只剔掉那一条**，
> 不再让一台坏机器身把整个库变成空的（以前历史里混进一个 `null` 会让整页变白）。

NAS 同步是可选的，需要你自己填 WebDAV 地址与账号。凭据只保存在本机
`localStorage`（键 `filmtap.webdav`），**不会写进代码，也不会随网页发布出去**。

统计页的数字（机身数、已用胶卷数、常用胶卷 Top 5）是**算出来的，不单独存**：
它们全部来自机身和库存这两份数据，所以只要这两份同步了，统计自动就对了，
不存在「统计忘了同步」这回事。

> ⚠️ 千万别把 NAS 地址或密码硬编码进源码。仓库是公开的，一旦提交，
> 密码就永久留在 Git 历史里 —— 删掉代码也没用，必须改密码 + 重写历史才能挽回。
> 这条不是假设，这个项目真踩过。`_selftest.js` 里有护栏盯着，但它只能拦形状。

## 数据存哪儿

三条路，可以同时开，也可以都不开。

| | 数据在哪 | 要配什么 | 本机被清空后 |
|---|---|---|---|
| 什么都不配 | 只在浏览器里 | 无 | 数据没了，靠导出的 JSON 手工恢复 |
| 私有仓库 | 你自己的私有 Git 仓库 | 一枚 fine-grained 令牌 | **打开时自动拉回来** |
| NAS | 你自己的 NAS | WebDAV 地址与账号 | 手动点「从 NAS 恢复」 |

### 为什么数据不能直接放进这个仓库

这个项目的代码仓库必须**公开** —— 免费账户的 GitHub Pages 只能从公开仓库发布，
仓库一转私有，站点就下线，所有标签全废。

而提交进 Git 的数据是**删不掉**的：它永远留在历史里，force-push 也没用
（GitHub 不会回收这些对象）。所以数据要单独放一个**私有**仓库，
代码和数据分开 —— 公开的继续公开，私有的真正私有。

### 配私有仓库（约五分钟，只做一次）

在 `#/settings` → 同步到私有仓库，三步：

1. **建一个私有仓库**（页面上有直达链接）：名字随意，比如 `film-tap-data`，里面什么都不用放。
2. **建一枚令牌**（页面上也有直达链接）：Settings → Developer settings →
   Personal access tokens → **Fine-grained tokens** → Generate new token
   - **Repository access**：选 *Only select repositories*，只勾①那个仓库
   - **Permissions** → Repository permissions → **Contents: Read and write**，
     其余全部留 *No access*
   - **Expiration**：设一个日期，别选 No expiration
3. **把令牌粘进「访问令牌」，点「验证并登录」**。应用会拿它问一次
   `GET /user` —— 通了就把**头像和用户名**显示出来当登录态，
   再顺手列出这枚令牌看得见的**私有仓库**，你从下拉里选一个，
   点「用这个仓库」就接上了。

选好之前和选好之后是两个状态：只有登录、没选仓库时，同步层（上传 / 拉回 / 自动同步）
**不会认这份配置**，所以不会拿半个配置去发请求。

**接上的那一刻，如果本机是空的，会自动把仓库里的数据拉回来** ——
这也就是「换了台新设备，粘一次令牌就能看到全部机身」的那条路。
（本机已经有数据时它一步都不动，一次远端都不读。）

第一次点「立即上传」会在仓库里建出 `data.json`。之后每次改数据都会**自动提交**一次，
而且 Git 的历史顺便成了你的版本管理 —— 能查某台机器什么时候换的卷。

> **列不出仓库不代表坏了。** fine-grained 令牌如果只勾了一个仓库，GitHub 可能不给列
> （甚至返回 403）。这种情况下展开「手动填仓库」，自己填所有者和仓库名，一样能用。

手机丢了、或者令牌泄露了：回 GitHub 把这枚令牌 **Delete** 掉即可。
删掉之后 App 就拉不动也推不了了，但仓库里的数据还在。

**令牌只保存在这台设备的 localStorage 里**，不进代码、不进仓库、也不随网页发布出去。
界面上也永远不会回显它（保存之后只显示「已保存，留空就不改」）。
`_selftest.js` 的护栏会拦下任何被写进源码的令牌形状，自检直接失败。

> 「自动同步」开关只管**上传**。关掉之后只有点「立即上传」才写仓库。
> 但「本机为空时自动拉回」不受这个开关影响 —— 那是防数据丢失的底线。
> `#/settings` 的「清空全部数据」会问你要不要连仓库里的一起清掉，
> 否则下次打开会被自动拉回来，看着像删不掉。

### ⚠️ 令牌和仓库地址是**一起被清掉**的

「本机为空就自动拉回」听着像是不用管，但它有个前提：**那份仓库配置还在**。
而仓库配置（`filmtap.github`）和数据（`filmtap.v1`）**住在同一个 localStorage 里** ——
Safari 按「7 天没交互」清理存储时，两个是**一起**没的。于是：

- 数据被清了，`getGh()` 跟着返回空，自动拉回**不会触发**；
- 界面上看起来就是「一个全新的空 App」。

所以真遇上这种情况，要做的不是重新录入机身，而是**回 `#/settings` 把令牌再粘一次**：
登录态恢复的同时，第一次打开就会把数据拉回来。设置页在未登录状态下也写了这句提示。

（NAS 那条路不受影响，因为凭据同样会被清 —— 区别只是它本来就要求你手动点「从 NAS 恢复」。）


### ⚠️ 两台设备同时开自动同步会互相覆盖

同步是**整库快照，后写的赢，没有合并**。而「自动拉回」只在**本机一台机身都没有**时
才动手（那是防丢数据的底线，不是同步机制）—— 所以第二台设备装好之后：

1. 它第一次打开会从仓库拉一份回来；
2. 之后两台各自往上推，谁后推谁赢，先推的那台**这次改动就没了**。

也就是说这不是「多端同步」，是「多端备份」。真要两台一起用，建议：

- 只在一台上开「自动同步」，另一台用「从仓库拉取」手动对齐；
- 或者干脆各记各的（比如一台管 135、一台管 120），别让两边都写同一个仓库。

（数据没真丢：仓库是 Git，历史里还留着被覆盖的那一版，能查回来。）

### 有一类上传会被直接拦下来

**「本机一台机身都没有、仓库里却有 N 台」这种上传会把仓库清空**，所以它被直接拒绝，
提示你先点「从仓库拉取」对齐。拦下来而不是问一句，是因为「自动同步」那条路没有人可问。

真实触发场景：本机存储被系统清理了（见下面 iPhone 那两条硬规则），
或者你换了台新设备打开 App、随手记了一台机身 —— 那一下就会把仓库里的二十台冲掉，
而且不报任何错。

真想清空仓库，走设置页的「清空全部数据」，那条路会明确问你要不要连仓库一起清。


## 目录

```
index.html            整个 App（单文件，零依赖，原生 JS）
sw.js                 Service Worker，离线可用
manifest.webmanifest  PWA 清单
_selftest.js          Node + DOM 桩，跑全部视图与数据流的断言
_shots.py             playwright 驱动本机 Edge 出界面截图 → shots/
shots/                界面截图，肉眼验收排版用
model/                3D 打印挂件
  build_fob.py        参数化建模 → fob.stl
  fob.scad            同一模型的 OpenSCAD 版（可压浮雕机身名）
  preview.py          出施工图 drawing.svg + 几何自检
  render.py           纯 numpy + Pillow 软件光栅渲染预览图
```

## 怎么用

线上地址（GitHub Pages，发布自 `main` 分支根目录，推一次自动更新）：

**https://escaper929.github.io/film-tap/**

NFC 标签里写的就是这个地址加上机身 ID：

```
https://escaper929.github.io/film-tap/?c=<机身ID>
```

`#/settings` 页的「已登记的机身 ID」会列出每台机身对应的完整 URL，直接复制即可。

本地预览：

```bash
python -m http.server 8080
```

> 直接双击 `index.html` 只能看界面 —— `file://` 下 Service Worker 不注册、离线缓存失效，
> 而且标签网址会算成 `file:///C:/...`，写进标签毫无意义。

打开后点「载入示例数据」可以先看看成品长什么样。

## iPhone 上的两条硬规则

这个 App 在 iPhone 上有两个绕不过去的行为，直接决定该怎么用。

> 下面这两条**只对 iPhone 成立**。Android 上把 PWA 装到桌面反而是加分项：
> 独立窗口、不受「闲置清理」那类策略影响，而且碰标签的行为也更可控。
> 换句话说，「别装到主屏」是 iOS 的存储隔离逼出来的规矩，不是这个 App 的偏好。

### 1. 碰标签只会打开 Safari，不会打开主屏 Web App

iOS 的后台读卡，只在标签里是**有效的 Apple Universal Link** 时才会把链接交给 App ——
那需要原生 App 用 Associated Domains 声明域名，并在站点上放
`apple-app-site-association`。我们是纯网页，没有原生 App，
所以 iOS 只当它是普通网址，**一律交给 Safari 打开**。

推论：**不要「添加到主屏幕」。** 装了不会更顺，反而会在手机上多出第二个数据库 ——
主屏 Web App 的存储与 Safari 是**互相隔离**的（iOS 把每次安装当成一个独立 origin）。
碰标签进去的是 Safari 那份数据，点主屏图标进去的是空的那份。
既然标签只能到 Safari，数据就应该住在 Safari 里。

### 2. Safari 里的数据闲置 7 天会被静默清空

Safari 的 ITP 会清掉「7 天内没有任何交互」的 origin 的全部脚本可写存储
（localStorage / IndexedDB / Cache API），没有报错、没有提示，下次打开就是空库。
装了主屏 App 的 origin 豁免这条，但按上一条我们不能用主屏 App。

偏偏这个 App 的全部价值就是「记住机身里装的什么卷」，正是最怕被清的那类数据。
四条对策，从最省心往下排：

- **配私有仓库（推荐）** —— 本机一台机身都没有时，打开 App 会**自动从仓库拉回来**。
  配置见上面「配私有仓库」。但那一步要花的力气是**回 GitHub 建一枚新令牌**，
  不是「重新验证一下」：清空存储时令牌和仓库地址也一起被清掉（见上一节），
  而 GitHub 只在创建那一刻显示一次令牌值，旧的那串你多半已经找不回来了。
  仍然是最省心的一条，只是别以为它能在你什么都不做的情况下自愈。
- **配 WebDAV 备份**（`#/settings`）—— 万一被清，一键「从 NAS 恢复」。
  WebDAV 的账号密码一样存在同一个 localStorage 里，被清也是一起没 ——
  但它的凭据是你自己设的、**你能记住**，可以原样再填一遍，
  不像 GitHub 令牌那样「一次性的、丢了只能重建」。
- 顺手「导出备份（JSON）」存进 iCloud Drive。`#/settings` 也有「导入备份」，
  两个容器之间、两台手机之间都靠它搬。**它不依赖任何令牌，是真正保命的那条路。**
- 哪怕什么都不配，**每周打开一次**也能重置计时器（碰任意一个标签即算一次交互）。
  注意「7 天」是从**最后一次交互**算起，不是从配好那天算 ——
  一周碰一次标签就够了。

## 一次性的准备（每台机身做一次）

推荐「先写卡，再碰一下登记」——这样机身 ID 由你自己定，好记也好念：

1. **挑一个短 ID** —— 小写字母数字，别重复：`m6` / `fm2` / `rollei35`
2. **写标签** —— 用 NFC Tools 写入一条 **URL** 记录：
   `https://escaper929.github.io/film-tap/?c=m6`
   必须是 URI 记录且放**第一条**：iOS 后台读取只认 URI 记录，多条时只用第一条，
   也不支持自定义 scheme（`film://` 这种写了也读不到）
3. **碰一下登记** —— 系统弹出通知 → 点开 → 页面显示「这个挂件还没登记」，
   填名称、品牌、型号、画幅 → 保存。以后碰这一下就直接落到这台机身
4. **打印挂件** —— `python model/build_fob.py` 出 `fob.stl`，层高 0.16mm，
   打完**第 17 层**（Z = 2.72mm）暂停放入标签，再封顶 2.24mm

> 机身 ID 也可以让 App 生成：首页「＋ 添加机身」会分配一个 `cam-xxxxx`，
> 再去 `#/settings` 复制完整标签 URL 写卡。两种都行，只是 `m6` 这种自己起的更好认。

标签只写这一次，之后换卷全在 App 里改。

## 日常

碰一下 → 直接落在那台机身：

- **换一卷** —— 选型号，张数按画幅预填（135 → 36，120 → 12）；回卷自动归档进「拍过的卷」
- **编辑这卷** —— 填 ISO 与实拍 EI，会自动给出推拉档位提示
- **复制冲扫备注** —— 一键生成能直接发给店家的说明（型号、EI、推拉、装卷日期）
- **库存自动扣** —— 有库存的前提下，这一卷装完就从 `#/films` 里减一卷
- 装得久了页面会标「偏久」，超过 180 天提示该冲了

这一页没有「过片 ＋1」是有意的。胶片机的计数器就在机身上，一低头就能看见；
在手机里再点一遍等于把同一个数字维护两次，而错误只会出现在更麻烦的那一次。
所以「换一卷」是这一页的主按钮 —— 它是这里唯一真正改变状态的动作。

## 胶卷库存

`#/films`。记的不是「机身里装的什么卷」（那是装卷记录的事），而是**柜子里还剩什么**。

一条库存记七个字段：型号、画幅、还有几卷、有效期、购入日期、单价、备注。
列表按**最快到期的排最前**，并给「已过期 / 本月到期 / 还剩 N 个月」的标签。

> **同一款卷在 135 和 120 上是两条库存。** 价格、张数都不一样，合成一条会算不清成本。
> 所以库存的唯一标识是「型号 + 画幅」，不是型号本身。
> 写型号时中文可以直接写（`富士 100`），不会被当成空值或跟别的款混淆。

装卷时会自动在这个库里**扣一卷**：装成功提示「库存还剩 N 卷」；
库里记的是 0 卷会提醒你对一下。编辑一条已存在的装卷记录**不会重复扣**。

### 从表格导入

`#/films/import`，三步：选文件 → 对列 → 确认。

表头**不用改**，认得出来就自动对：型号 / 数量 / 画幅 / 有效期 / 购入日期 / 单价 / 备注。
同义写法也认，例如型号那列写「胶卷型号」「品名」「film stock」都行，
数量写「存量」「剩余卷数」「rolls」，有效期写「到期」「保质期」「expiry」。
认不准的那一列会在第二步让你用下拉手工指定，**不靠猜**。

- 支持 **CSV / TSV / 直接用 Excel 粘贴**。制表符、分号、竖线分隔都能嗅探出来，
  字段里带逗号（`"Fuji, 分装"`）、带引号、带 BOM 都处理了。
  嗅探会看**前几行**而不是只看首行 —— 真实表格第一行经常是个跨列大标题，
  只看首行的话制表符文件会被判成逗号，整张表就废了。
- 中文 Windows 上 Excel 另存的 CSV 默认是 **GBK**，不是 UTF-8 —— 两种编码都能读。
- **`.xlsx` 读不了**（它是个压缩包，浏览器里解不了），请先在 Excel 里「另存为 CSV」。
- 「已有的同款怎么办」：**覆盖** = 这张表当权威全量，第一次整份导入用这个；
  **累加** = 这张表当新买的一批，跟现有存量相加。
- 文件只在这台设备上解析，不会上传到任何地方。

> 库存和机身一样是数据库的一个顶层字段，所以它自动获得上传、回灌、
> JSON 导出导入、NAS 同步 —— 同步层不认识业务字段，加了新字段不用改同步代码。

## 自检与截图

```bash
node _selftest.js      # 全部视图渲染 + 张数 / 装卷 / 库存与导入 / 改名迁移 / 凭据护栏
python _shots.py       # 需先起本地服务，截图输出到 shots/
python _firstrun.py     # 打真实部署的页面，走一遍「空库首次使用」，不用起服务
```

> `_selftest.js` 是在内存里造数据 + 直接跳哈希，从来不点按钮、不进表单、
> 不经过 Service Worker —— 于是「空库 → 登记第一台机身 → 装卷 → 刷新 → 数据还在」
> 这条**每个新用户唯一会走的**路径，在自检里是完全空白的。
> `_firstrun.py` 就是补这块：用真浏览器在线上页面点一遍，
> 顺带确认线上拿到的确实是新版（`sw.js` 有没有同步）。

自检还钉住了七条容易悄悄坏掉的性质：

- **机身 ID 不能逃出 `onclick`**。ID 从 URL 的 `?c=` 来（就是 NFC 标签里的内容），
  又会被拼进 `onclick`。断言不是「字符串里有没有那几个字」——转义之后那几个字还在，
  那种断言两边都通过。做法是把渲染出的每个 `onclick` **真的喂给 JS 引擎跑一遍**，
  看它是不是只调了一次处理函数、有没有碰到 `document`。
  （设置页有三套排版：未登录 / 登录了没选仓库 / 已登录，三套都要各扫一遍。）
- **登录只说明「这枚令牌是谁的」，不等于选好了仓库** —— 验证通过但还没选仓库时，
  同步层必须不认这份配置，否则会拿缺 `owner` / `repo` 的配置去拼 API 地址。
- **列不出仓库不能把人卡死** —— fine-grained 令牌只勾一个仓库时 GitHub 可能不给列，
  这条路必须能退回手填，而且手填时不该被要求再粘一次刚粘过的令牌。
- **读不出来的数据要留底**（`filmtap.v1.corrupt`），后面来的写入不能顶掉它。
- **删掉的功能要真的是删干净了**。删功能留下的往往不是报错，是半截残骸：
  某个入口没摘掉、某个视图还在算一个不会变的数、或者老数据里的字段还在被同步来同步去。
  所以这里正面断言「函数没了、页面里没有残留、表单里没有那个输入框」，
  以及**老库里的 `shots` 会在 `normalizeRoll` 里被清掉**（`total` 必须留下 ——
  那是「这卷多少张」，和「已拍几张」是两回事）。
- **历史里的坏记录只剔那一条**，不能让整页变白。
- **点「同步到 NAS」不触达私有仓库** —— 断言落在「那个 PUT 去了哪个域名」上。

> ⚠️ 加新的可点击元素时记住：往 `onclick` 里塞数据要过 `jsq()`（跳转走 `goAttr()` /
> `tapRow()`）。`esc()` 在这件事上不管用 —— 它把 `'` 变成 `&#39;`，
> HTML 解析器读属性时又会还原成 `'`，字符串照样被闭掉。
> `encodeURIComponent` 也不行，它不编码 `! ' ( ) *`。

`_selftest.js` 里有一道**凭据护栏**，扫描仓库里所有发得出去的文本文件（不只是站点上
打得开的那些 —— 上次泄露就发生在这个自检文件里，而 Pages 会跳过下划线开头的文件，
所以「站点上 404」根本不等于「仓库里拿不到」）。只要出现内网网段、Basic 认证头、
GitHub 令牌形状，或把凭据直接写成常量赋值，自检直接失败，不通过。

> 护栏自己的样本也只在运行时拼接，绝不把真实值或可命中的形状写成字面量 ——
> 否则护栏本身就成了新的泄露点。这个坑踩过。

### 改完 `index.html` 必须同步改 `sw.js`

Service Worker 取文件是「缓存优先」，而浏览器只靠 **`sw.js` 的字节变化**
来判断要不要装新 SW。所以只改 `index.html` 不动 `sw.js`，线上是新版、
手机上却一直停在旧版界面，**而且不报任何错**。这个坑踩过。

现在由自检强制：`sw.js` 里存着 shell 文件（`index.html` + `manifest.webmanifest`）
的 SHA-256，自检会算一遍比对。对不上就失败，并把正确的值打出来让你照抄进
`sw.js` —— 那一行一改，`sw.js` 的字节就变了，浏览器才终于会去装新 SW。
所以改 `index.html` 之后的固定动作是：跑一次 `node _selftest.js`，
按提示更新 `SHELL_FP` 并把 `CACHE` 的版本号 +1。

`_selftest.js` 的驱动整体是一个 `String.raw` 模板字符串 —— 里面写
`\n`、`\r`、`\d` 都不用再翻倍。改用 `String.raw` 之前，每写一个反斜杠
都得写两遍，漏一个就报「Invalid or unexpected token」还没行号。

## 硬件

挂件是 3D 打印的，中间留了 NTAG213 / 215 / 216 圆片标签的腔体：
打到指定层高暂停 → 放入标签 → 继续封顶。

两个硬约束：**别用磁铁固定**（金属会让 NFC 直接失效）、
**别平贴金属机身**（天线失谐）。走穿绳孔挂在背带上。

## 历史

上一代是 `film-camera-tracker`（胶片管家）：纯手动索引、没有 NFC 概念。
这一版把它的统计页、机身结构化字段、NAS 同步吸收进来，换成 NFC 直达的架构重做。
（它的过片计数没有再收 —— 见上面「这里不数快门」。）

项目名统一成 `film-tap` 之前叫过 `film-nfc`，界面标题叫过「胶片簿」——
仓库名、目录名、界面名已经全部统一，不再是三个名字。
